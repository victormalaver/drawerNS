<Page xmlns="http://schemas.nativescript.org/tns.xsd" xmlns:widgets="utils/widgets" loaded="pageLoaded">
  <Page.actionBar>
    <widgets:action-bar />
  </Page.actionBar>

  <StackLayout orientation="vertical">

    <TextField id="email" text="{{ email }}" hint="Email Address" keyboardType="email" autocorrect="false" autocapitalizationType="none" />
    <TextField secure="true" text="{{ password }}" hint="Password" />

    <Button text="Iniciar Sesión" tap="signIn" />
    <Button text="Registrarme" class="link" tap="register" />

  </StackLayout>
</Page> 